{
  "name": "gamified-casual-game",
  "version": "1.0.0",
  "description": "Gamified casual game for teens (React + Firebase) — deploy Heroku",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "engines": {
    "node": ">=18.x"
  }
}
